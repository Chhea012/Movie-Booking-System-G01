class Cinema {
    constructor(
        private cinemaId: string,
        private name : string,
        private address : string
    ){
        this.cinemaId = cinemaId;
        this.name = name;
        this.address = address;
    }
}